# 2026-05-21 进展日报

## 今日目标

补齐 P0 clean strict 主评测的标准脚本入口，避免论文复现实验继续依赖口头映射说明。

## 修改文件

- `scripts/eval_clean_strict.py`：新增薄 wrapper，直接复用 `scripts/evaluate_models.py` 的 CLI 和核心评测逻辑。

## 运行命令

```bash
python -m py_compile scripts/eval_clean_strict.py scripts/evaluate_models.py
python scripts/eval_clean_strict.py --help
```

## 关键结果

- `eval_clean_strict.py` 可正常导入并展示与 `evaluate_models.py` 相同的参数入口。
- 默认不传 `--use-multi-answer-negatives`，因此 `select_gold_neg` 仍按 hard single-gold NegRank 统计。
- 若需要 Multi-answer NegRank，仍需显式传入 `--use-multi-answer-negatives`，口径与原 evaluator 完全一致。

## 是否进入论文

建议进入 reproducibility appendix，作为 clean strict 主评测的固定命令入口；不作为实验结果新增。

## 风险和下一步

- 风险：这是入口补齐，不产出新的数值结果，也未验证真实模型评测全链路。
- 下一步：在有模型与 clean strict 数据路径的环境中，用该入口复现主表 JSON，并继续补齐 WikiFact / BoolQ / MMLU 的标准 wrapper。
