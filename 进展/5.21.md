# 2026-05-21 进展日报

## 今日目标

为 P0 clean strict 主评测补齐标准脚本入口，便于后续训练与主表命令统一引用 `scripts/eval_clean_strict.py`。

## 修改文件

- `scripts/eval_clean_strict.py`
- `进展/5.21.md`

## 运行命令

```bash
python scripts/eval_clean_strict.py --help
python -m py_compile scripts/eval_clean_strict.py scripts/evaluate_models.py
```

## 关键结果

- 新增 `scripts/eval_clean_strict.py` 薄 wrapper，直接复用 `scripts/evaluate_models.py` 的 `main()`。
- 默认不传 `--use-multi-answer-negatives`，因此 clean strict 入口保持 hard single-gold `NegRankAcc` 口径。
- Multi-answer 诊断仍需显式传入 `--use-multi-answer-negatives`，不会混入默认主评测。

## 是否进入论文

作为 reproducibility / release artifact 入口进入附录命令说明；本轮不产生新实验数字，不进入结果表。

## 风险和下一步

- 风险：该 wrapper 只标准化入口，不绑定具体 clean strict 数据路径；实际主表仍需要调用方明确传入固定 split 和模型配置。
- 下一步：在有固定 clean strict eval JSON 后，可继续补 `aggregate_main_tables.py` 或主表导出命令，确保 JSON/CSV/Markdown 可重现。
