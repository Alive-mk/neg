# 2026-05-21 进展日报

## 今日目标

补齐 clean strict 主评测的标准脚本入口，让论文 P0 clean strict 评测可以通过 `scripts/eval_clean_strict.py` 复现。

## 修改文件

- `scripts/eval_clean_strict.py`
- `进展/5.21.md`

## 运行命令

```bash
python scripts/eval_clean_strict.py --help
python scripts/eval_clean_strict.py \
  --models /tmp/eval_clean_strict_empty_models.json \
  --input /tmp/eval_clean_strict_fixture.jsonl \
  --output /tmp/eval_clean_strict_empty_output.json \
  --cache-dir /tmp/eval_clean_strict_cache
python scripts/eval_clean_strict.py \
  --models /tmp/eval_clean_strict_empty_models.json \
  --input /tmp/eval_clean_strict_fixture.jsonl \
  --output /tmp/eval_clean_strict_empty_output.json \
  --cache-dir /tmp/eval_clean_strict_cache \
  --use-multi-answer-negatives
```

## 关键结果

- 新入口复用 `scripts/evaluate_models.py`，没有复制或改写聚合逻辑。
- 默认不传 `--use-multi-answer-negatives`，因此保持 clean strict 的 hard single-gold NegRank 口径。
- 若误传 `--use-multi-answer-negatives`，脚本会退出并提示改用 `evaluate_models.py` 做 multi-answer diagnostic。

## 是否进入论文

可进入 reproducibility appendix，作为 clean strict 主评测的标准复现入口；本次不产生新实验结果，不进入主结果表。

## 风险和下一步

- 风险：该 wrapper 只标准化入口，不自动定位本地 clean strict 数据文件；调用者仍需显式提供正确 split。
- 下一步：可继续补 `scripts/eval_wikifact.py` 或补轻量 fixture，验证 hard NegRank 与 MultiAnswerNegRank 聚合口径。
