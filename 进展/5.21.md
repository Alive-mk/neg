# 2026-05-21 进展日报

## 今日目标

补齐 P0 clean strict 主评测的标准入口：新增薄 wrapper `scripts/eval_clean_strict.py`，复用 `scripts/evaluate_models.py`，默认保持 hard single-gold NegRank 口径。

## 修改文件

- `scripts/eval_clean_strict.py`
  - 新增 clean strict 默认输入：`data/processed/validated_largetest_v2_clean_strict.jsonl`
  - 不默认添加 `--use-multi-answer-negatives`，因此 `NegRankAcc` 仍是 hard single-gold。
  - 不默认设置 `--neg-prefix auto`，oracle behavior-token 评测仍需调用者显式传入，避免 wrapper 偷改 token 条件。

## 运行命令

```bash
python scripts/eval_clean_strict.py --help
python -m py_compile scripts/eval_clean_strict.py scripts/evaluate_models.py
```

## 关键结果

- `--help` 可正常显示 wrapper 说明和底层 `evaluate_models.py` 参数。
- `py_compile` 通过。
- 未运行重模型评测，避免在本轮自动推进中消耗 GPU 或生成大输出。

## 是否进入论文

不直接进入论文结果表；属于 reproducibility / standard script entrypoint 补强，可服务 clean strict 主表复现。

## 风险和下一步

- 风险：默认输入文件位于 ignored 的本地 `data/` 目录，本 worktree 不包含该数据文件；实际评测仍需在完整实验环境运行。
- 风险：wrapper 仅补标准入口，不替代 multi-answer audit；多答案诊断仍需显式传入 `--use-multi-answer-negatives` 或使用 reaggregation/audit 脚本。
- 下一步：可继续补 `eval_wikifact.py` / `eval_boolq.py` / `eval_router.py` 标准入口，或为 reaggregate hard vs multi-answer 口径增加轻量 fixture。
