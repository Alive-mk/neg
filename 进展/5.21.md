# 2026-05-21 进展日报

## 今日目标

补齐 P0 clean strict 主评测的标准入口脚本名，便于后续按 `scripts/eval_clean_strict.py` 复现实验命令。

## 修改文件

- `scripts/eval_clean_strict.py`：新增薄 wrapper，直接复用 `scripts/evaluate_models.py` 的 `main()`。

## 运行命令

```bash
python scripts/eval_clean_strict.py --help
python -m py_compile scripts/eval_clean_strict.py scripts/evaluate_models.py
```

## 关键结果

- `eval_clean_strict.py` 只提供标准入口名，不重新实现评测逻辑。
- 默认仍沿用 `evaluate_models.py` 的 hard single-gold 口径；`--use-multi-answer-negatives` 仍为显式 opt-in，因此不改变 Hard NegRank。
- 未运行重模型评测，未生成或提交 `data/`、`outputs/`、`model/` 产物。

## 是否进入论文

不作为论文结果进入主表；可进入 reproducibility appendix 或 release README，作为 clean strict 主评测入口说明。

## 风险和下一步

- 风险：该 wrapper 不内置 clean strict 数据路径，仍需调用者显式传入固定 clean strict split。
- 下一步：在数据 release 路径稳定后，可在 README 或 reproducibility appendix 中给出完整可复制命令。
