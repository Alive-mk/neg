## 今日目标

为 P0 clean strict 主评测补齐标准脚本入口 `scripts/eval_clean_strict.py`，复用现有 `scripts/evaluate_models.py`，避免新增或漂移指标实现。

## 修改文件

- `scripts/eval_clean_strict.py`：新增 thin wrapper，直接调用 `evaluate_models.main()`。

## 运行命令

```bash
python -m py_compile scripts/eval_clean_strict.py scripts/evaluate_models.py
python scripts/eval_clean_strict.py --help
```

## 关键结果

- wrapper 可导入并展示与 `evaluate_models.py` 等价的 CLI。
- 默认不传 `--use-multi-answer-negatives`，因此 SELECT 仍按 hard single-gold `NegRankAcc` 作为主口径。
- Multi-answer 诊断仍需显式传入 `--use-multi-answer-negatives`，不影响主表 hard 口径。

## 是否进入论文

不直接进入论文结果表；这是复现入口补齐，服务于 clean strict 主评测的可执行性和审计性。

## 风险和下一步

- 风险：该 wrapper 只补入口，不自动固定模型配置、输入文件或 behavior token 策略；实际主表仍需在命令或上层脚本中明确 `--models`、`--input`、`--neg-prefix` 和 `--model-names`。
- 下一步：继续补齐 WikiFact、BoolQ、MMLU 等标准入口，或增加 clean strict fixture 验证 hard NegRank 与 MultiAnswerNegRank 的聚合口径。
